export interface station {
  id: number,
  number: number,
  name: string,
  date: string,
  from: string,
  to: string,
  departure: string,
  arrive: string,
  departureId: number,
  vagons : any[]
}
